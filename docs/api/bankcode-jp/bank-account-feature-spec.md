# 銀行口座情報登録機能 仕様書

> ワーカーの銀行口座情報を登録・管理する機能の設計仕様

## 1. 概要

### 1.1 目的

ワーカーが報酬を受け取るための銀行口座情報を登録・管理できる機能を提供する。
金融機関コード・支店コードの入力を補助するため、ハイブリッド検索機能を実装する。

### 1.2 対象ユーザー

- ワーカー（Worker）

### 1.3 データソース（ハイブリッド方式）

| 優先度 | データソース | 用途 | APIコール |
|--------|--------------|------|-----------|
| 1st | **ローカルDB** | 高速検索、主要銀行 | 0 |
| 2nd | **BankcodeJP API** | 最新データ補完 | ユーザー操作時のみ |

#### ローカルデータ
- **ソース**: [zengin-code/source-data](https://github.com/zengin-code/source-data)
- **ライセンス**: MIT
- **更新**: Vercel Cronで週1回自動取得
- **保存先**: PostgreSQL（banks, branches テーブル）

#### フォールバックAPI
- **BankcodeJP API v3**
- ベースURL: `https://apis.bankcode-jp.com/v3/`
- 認証: 環境変数 `BANKCODEJP_API_KEY`
- 用途: ローカルで見つからない場合の補完

---

## 2. 機能要件

### 2.1 銀行口座登録

| 項目 | 必須 | 形式 | 説明 |
|------|------|------|------|
| 金融機関名 | ○ | 文字列 | 検索で選択 |
| 金融機関コード | ○ | 4桁数字 | 自動入力 |
| 支店名 | ○ | 文字列 | 検索で選択 |
| 支店コード | ○ | 3桁数字 | 自動入力 |
| 口座種別 | ○ | 選択 | 普通/当座 |
| 口座番号 | ○ | 7桁数字 | 手動入力 |
| 口座名義（カナ） | ○ | 全角カナ | 手動入力 |

### 2.2 金融機関検索

- ローカルDB優先のあいまい検索
- リアルタイム候補表示（即時、デバウンスなし）
- 最大表示件数: 10件
- ローカルで見つからない場合は「最新データで検索」オプション表示

### 2.3 支店検索

- 選択した金融機関の支店のみ検索
- ローカルDB優先
- ローカルで見つからない場合のみAPI検索

---

## 3. 画面仕様

### 3.1 初期表示（主要銀行クイック選択）

```
┌─────────────────────────────────────────┐
│ 振込先口座を登録                        │
├─────────────────────────────────────────┤
│                                         │
│ 金融機関を選択                          │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 銀行名を入力...                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ▼ よく利用される銀行                    │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │ みずほ  │ │三菱UFJ │ │三井住友│   │
│ └─────────┘ └─────────┘ └─────────┘   │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │ゆうちょ│ │  楽天   │ │ PayPay │   │
│ └─────────┘ └─────────┘ └─────────┘   │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │住信SBI │ │ イオン  │ │auじぶん│   │
│ └─────────┘ └─────────┘ └─────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 3.2 検索入力時（ローカル検索）

```
┌─────────────────────────────────────────┐
│ 金融機関を選択                          │
├─────────────────────────────────────────┤
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 ちば                        ✕   │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🏦 千葉銀行                    0134 │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏦 千葉興業銀行                0135 │ │
│ ├─────────────────────────────────────┤ │
│ │ 🏦 京葉銀行                    0522 │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ 💡 お探しの銀行が見つからない場合       │
│    → 最新データで検索                   │
│                                         │
└─────────────────────────────────────────┘

※ この時点でAPIコールは0回
```

### 3.3 ローカルで見つからない場合

```
┌─────────────────────────────────────────┐
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 〇〇信用組合                 ✕   │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ │   該当する金融機関が見つかりません  │ │
│ │                                     │ │
│ │   ┌───────────────────────────────┐ │ │
│ │   │  🔄 最新データで検索する     │ │ │
│ │   └───────────────────────────────┘ │ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘

↓ ユーザーが「最新データで検索する」をタップ

┌─────────────────────────────────────────┐
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 〇〇信用組合                 ✕   │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ⏳ 最新データを検索中...                │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🏦 〇〇信用組合                2XXX │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘

※ この時点でAPIコール1回
```

### 3.4 支店選択

```
┌─────────────────────────────────────────┐
│ 支店を選択                              │
├─────────────────────────────────────────┤
│                                         │
│ 選択中: みずほ銀行                      │
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │ 🔍 支店名を入力...                  │ │
│ └─────────────────────────────────────┘ │
│                                         │
│ ┌───────────────────────────────┐       │
│ │ 東京営業部              001   │       │
│ ├───────────────────────────────┤       │
│ │ 丸の内中央支店          004   │       │
│ ├───────────────────────────────┤       │
│ │ 新宿支店                110   │       │
│ ├───────────────────────────────┤       │
│ │ 渋谷支店                120   │       │
│ └───────────────────────────────┘       │
│                                         │
│ 💡 支店が見つからない場合               │
│    → 最新データで検索                   │
│                                         │
└─────────────────────────────────────────┘
```

### 3.5 口座情報入力

```
┌─────────────────────────────────────────┐
│ 口座情報を入力                          │
├─────────────────────────────────────────┤
│                                         │
│ 選択中: みずほ銀行 東京営業部           │
│                                         │
│ 口座種別 *                              │
│ ┌──────────────┐ ┌──────────────┐       │
│ │ ◉ 普通      │ │ ○ 当座      │       │
│ └──────────────┘ └──────────────┘       │
│                                         │
│ 口座番号 *                              │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│ 7桁の数字を入力してください             │
│                                         │
│ 口座名義（カナ） *                      │
│ ┌─────────────────────────────────────┐ │
│ │                                     │ │
│ └─────────────────────────────────────┘ │
│ 通帳に記載されている名義をカタカナで入力│
│                                         │
│ ┌─────────────────────────────────────┐ │
│ │            登録する                 │ │
│ └─────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

### 3.6 入力バリデーション

| フィールド | バリデーション | エラーメッセージ |
|------------|----------------|------------------|
| 金融機関 | 必須、選択式 | 金融機関を選択してください |
| 支店 | 必須、選択式 | 支店を選択してください |
| 口座種別 | 必須 | 口座種別を選択してください |
| 口座番号 | 必須、7桁数字 | 口座番号は7桁の数字で入力してください |
| 口座名義 | 必須、全角カタカナ | 口座名義はカタカナで入力してください |

---

## 4. データベース設計

### 4.1 Prismaスキーマ

```prisma
// ========================================
// 金融機関マスタ（zengin-codeから定期取得）
// ========================================
model Bank {
  code      String   @id @db.VarChar(4)
  name      String   @db.VarChar(100)
  kana      String   @db.VarChar(100)
  hira      String   @db.VarChar(100)
  roma      String?  @db.VarChar(100)
  branches  Branch[]
  updatedAt DateTime @updatedAt

  @@map("banks")
}

// ========================================
// 支店マスタ（zengin-codeから定期取得）
// ========================================
model Branch {
  id        String   @id @default(cuid())
  bankCode  String   @db.VarChar(4)
  code      String   @db.VarChar(3)
  name      String   @db.VarChar(100)
  kana      String   @db.VarChar(100)
  hira      String   @db.VarChar(100)
  roma      String?  @db.VarChar(100)
  bank      Bank     @relation(fields: [bankCode], references: [code], onDelete: Cascade)
  updatedAt DateTime @updatedAt

  @@unique([bankCode, code])
  @@index([bankCode])
  @@map("branches")
}

// ========================================
// ワーカー銀行口座情報
// ========================================
model BankAccount {
  id                String      @id @default(cuid())
  workerId          String      @unique
  worker            Worker      @relation(fields: [workerId], references: [id], onDelete: Cascade)

  bankCode          String      @db.VarChar(4)
  bankName          String      @db.VarChar(100)
  branchCode        String      @db.VarChar(3)
  branchName        String      @db.VarChar(100)
  accountType       AccountType @default(ORDINARY)
  accountNumber     String      @db.VarChar(7)
  accountHolderName String      @db.VarChar(100)

  createdAt         DateTime    @default(now())
  updatedAt         DateTime    @updatedAt

  @@map("bank_accounts")
}

enum AccountType {
  ORDINARY  // 普通
  CURRENT   // 当座
}
```

### 4.2 マイグレーション

```bash
npx prisma migrate dev --name add_bank_master_and_accounts
```

---

## 5. API設計

### 5.1 銀行検索API（ローカルDB）

```typescript
// GET /api/bank/search?q=みずほ
interface BankSearchResponse {
  banks: {
    code: string;      // "0001"
    name: string;      // "みずほ"
    kana: string;      // "ミズホ"
    hira: string;      // "みずほ"
  }[];
  source: 'local' | 'api';  // データソース
}
```

### 5.2 銀行検索API（BankcodeJP API）

```typescript
// GET /api/bank/search?q=〇〇信金&source=api
// ユーザーが「最新データで検索」をタップした時のみ使用
```

### 5.3 支店検索API

```typescript
// GET /api/bank/{bankCode}/branches?q=東京
interface BranchSearchResponse {
  branches: {
    code: string;      // "001"
    name: string;      // "東京営業部"
    kana: string;      // "トウキヨウ"
    hira: string;      // "とうきよう"
  }[];
  source: 'local' | 'api';
}
```

### 5.4 口座情報保存API

```typescript
// POST /api/worker/bank-account
interface SaveBankAccountRequest {
  bankCode: string;
  bankName: string;
  branchCode: string;
  branchName: string;
  accountType: 'ORDINARY' | 'CURRENT';
  accountNumber: string;
  accountHolderName: string;
}

interface SaveBankAccountResponse {
  success: boolean;
  bankAccount?: {
    id: string;
    bankCode: string;
    bankName: string;
    branchCode: string;
    branchName: string;
    accountType: string;
    accountNumber: string;
    accountHolderName: string;
  };
  error?: string;
}
```

### 5.5 主要銀行リストAPI

```typescript
// GET /api/bank/major
// 初期表示用の主要銀行リスト（9行）
interface MajorBanksResponse {
  banks: {
    code: string;
    name: string;
  }[];
}
```

---

## 6. 定期データ更新

### 6.1 Vercel Cron Job

```typescript
// app/api/cron/update-bank-data/route.ts

export const runtime = 'nodejs';
export const maxDuration = 300; // 5分

export async function GET(request: Request) {
  // 認証確認
  const authHeader = request.headers.get('authorization');
  if (authHeader !== `Bearer ${process.env.CRON_SECRET}`) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
  }

  // 1. GitHubからbanks.json取得
  const banksRes = await fetch(
    'https://raw.githubusercontent.com/zengin-code/source-data/master/data/banks.json'
  );
  const banksData = await banksRes.json();

  // 2. DBにupsert
  for (const [code, bank] of Object.entries(banksData)) {
    await prisma.bank.upsert({
      where: { code },
      update: { name: bank.name, kana: bank.kana, hira: bank.hira, roma: bank.roma },
      create: { code, name: bank.name, kana: bank.kana, hira: bank.hira, roma: bank.roma },
    });
  }

  // 3. 各銀行の支店データを取得・保存
  for (const code of Object.keys(banksData)) {
    const branchesRes = await fetch(
      `https://raw.githubusercontent.com/zengin-code/source-data/master/data/branches/${code}.json`
    );
    if (branchesRes.ok) {
      const branchesData = await branchesRes.json();
      // 支店データをupsert...
    }
  }

  return NextResponse.json({ success: true });
}
```

### 6.2 vercel.json設定

```json
{
  "crons": [
    {
      "path": "/api/cron/update-bank-data",
      "schedule": "0 3 * * 0"
    }
  ]
}
```

**スケジュール**: 毎週日曜 3:00 AM (UTC)

### 6.3 環境変数

```bash
# .env.local
CRON_SECRET=ランダムな文字列（32文字以上）
BANKCODEJP_API_KEY=既存のAPIキー
```

---

## 7. 検索ロジック

### 7.1 銀行検索フロー

```typescript
// lib/bank-search.ts

export async function searchBanks(
  query: string,
  useApi: boolean = false
): Promise<BankSearchResult> {

  if (!useApi) {
    // 1. ローカルDB検索（デフォルト）
    const localResults = await prisma.bank.findMany({
      where: {
        OR: [
          { name: { contains: query } },
          { hira: { contains: query } },
          { kana: { contains: query } },
        ],
      },
      take: 10,
    });

    return {
      banks: localResults,
      source: 'local',
      showApiSearchHint: localResults.length === 0,
    };
  }

  // 2. BankcodeJP API検索（ユーザーが明示的に要求）
  const apiResults = await searchBanksFromAPI(query);
  return {
    banks: apiResults,
    source: 'api',
    showApiSearchHint: false,
  };
}
```

### 7.2 APIコール発生パターン

| ユーザー行動 | APIコール | 理由 |
|--------------|-----------|------|
| 主要銀行をタップ選択 | 0 | ローカルDB |
| 「千葉銀行」と検索 → ヒット | 0 | ローカルDB |
| 「〇〇信金」検索 → ヒット | 0 | ローカルDB |
| 「〇〇信金」検索 → ない → 最新検索タップ | 1 | ユーザー明示操作 |
| 新設銀行を検索 → ない → 最新検索タップ | 1 | ユーザー明示操作 |

**ポイント**: APIコールは**ユーザーの明示的な操作**でのみ発生

---

## 8. セキュリティ考慮事項

### 8.1 APIキー保護

- 環境変数で管理（`BANKCODEJP_API_KEY`）
- サーバーサイドでのみ使用（クライアントに露出させない）
- Vercel環境変数に設定

### 8.2 Cron認証

- `CRON_SECRET` による認証
- Vercel Cronからの呼び出しのみ許可

### 8.3 口座情報保護

- HTTPS通信必須
- 認証済みユーザーのみアクセス可能
- 自分の口座情報のみ参照・更新可能

### 8.4 入力サニタイズ

- SQLインジェクション対策（Prisma使用）
- XSS対策（React標準エスケープ）

---

## 9. エラーハンドリング

### 9.1 BankcodeJP APIエラー

| エラー | 対応 |
|--------|------|
| 401 | APIキー無効 → 管理者に通知 |
| 429 | レート制限 → 「しばらくお待ちください」表示 |
| 500 | サーバーエラー → リトライ（3回まで） |

### 9.2 ユーザー入力エラー

| エラー | メッセージ |
|--------|------------|
| 金融機関未選択 | 金融機関を選択してください |
| 支店未選択 | 支店を選択してください |
| 口座番号不正 | 口座番号は7桁の数字で入力してください |
| 口座名義不正 | 口座名義はカタカナで入力してください |

---

## 10. 実装計画

### Phase 1: データ基盤

1. Prismaスキーマ追加（Bank, Branch, BankAccount）
2. マイグレーション実行
3. 初期データ投入スクリプト作成・実行
4. Vercel Cron Job実装

### Phase 2: API実装

1. 銀行検索API（ローカル + APIフォールバック）
2. 支店検索API
3. 主要銀行リストAPI
4. 口座情報保存API

### Phase 3: フロントエンド実装

1. 銀行選択コンポーネント（プログレッシブ検索UI）
2. 支店選択コンポーネント
3. 口座情報入力フォーム
4. バリデーション実装

### Phase 4: テスト・調整

1. E2Eテスト作成
2. エラーハンドリング確認
3. UX改善・パフォーマンス最適化

---

## 11. 参考資料

- [BankcodeJP API v3 リファレンス](./api-reference.md)
- [zengin-code/source-data](https://github.com/zengin-code/source-data)
- [Vercel Cron Jobs](https://vercel.com/docs/cron-jobs)
- [システム設計書](../../specifications/system-design.md)
- [画面仕様書](../../specifications/screen-specification.md)
