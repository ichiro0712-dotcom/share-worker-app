# System Advisor 機能拡張 引き継ぎ (2026-05-16)

## このセッションのスコープ

`feature/advisor-sql-tool` ブランチで System Advisor に大規模機能追加を実施。

### 完成した機能

1. **execute_sql ツール** — LLMが任意のSELECT文を実行
   - 多層防御: SELECT/WITH限定、危険キーワード拒否、複文禁止、センシティブカラムブロック、LIMIT自動付与、statement_timeout=10s、READ ONLY
   - 表IDを `T-001` 形式で採番、`advisor_chat_tables` に保存
   - 監査ログを `advisor_sql_audit_logs` に記録

2. **SQL承認モーダル** — 実行前に必ずユーザー承認
   - 目的(purpose)表示、SQL本文は折りたたみ
   - 「セッション中はスキップ」チェックでセッション内自動承認
   - 承認状態バナー + 解除リンク (sqlAutoApprove ON時に表示)

3. **add_tables_to_report ツール** — T-XXXをレポートドラフトに追加
   - ボタン押下/チャット指示の両方から呼べる
   - 既存ドラフトがあれば末尾追記、無ければ新規作成

4. **表の共有メニュー** — Sheetsコピー / レポート送信 / URL共有
   - URL共有は `/advisor/t/[token]` 公開ページ (30日有効)
   - `advisor_chat_tables` に share_token/shared_at/shared_until を追加

5. **セッション跨ぎ参照** — 別セッションの表をDBから引いて表示
   - chat-layout の `fetchAndMapSessionTables` で時系列マッピング (C案: 表のcreated_atより前で最新のassistantメッセージに紐付け)
   - お気に入りなしセッションは30日後にcronで削除

6. **get_table ツール** — LLMが過去のT-XXXを参照
   - 「T-005 の合計を出して」→ DB取得 → 集計

7. **GA4 pageTraffic レポート** — ページ×流入元のクロス集計
   - `query_ga4(pageTraffic, page_path_prefix="/lp/30")` のような呼び出し
   - 「LP30 にどこから来た」に対応

8. **Markdown表のT-XXX後付け採番** (`markdown-table-extractor.ts`)
   - assistantメッセージ永続化後、Markdown表を検出してDB登録
   - `**表 T-XXX** (N 行)` プレフィックスを本文に挿入
   - これで GA4/query_metric 等の表も後から get_table / add_tables_to_report / 共有 対象になる

9. **execute_sql 後の loop=1 短絡** — TTFB 100秒問題回避
   - Anthropic API の TTFB 100秒問題対策。execute_sql 成功時はサーバー側固定文で即doneを返す

10. **doneイベントに annotatedContent を含める** — リロード不要でT-XXX反映
   - サーバー側で書き換えた本文を done で返却 → クライアントがstateを更新

11. **システムプロンプト改修**
   - get_table / execute_sql / GA4 pageTraffic の使い方を明示
   - 「取れない理由」の伝え方ルール (個人情報・未実装・データなし)

12. **Search Console (`SEARCH_CONSOLE_SITE_URL`) 環境変数設定** — `.env.local` のみ
   - 現状: `https://tastas.work/` (URLプレフィックスプロパティ用)
   - **ただし認証エラーで動いていない (後述)**

13. **GA4 サービスアカウントキー再発行**
   - `credentials/ga-service-account.json` に新キー配置済み
   - GA4 は動作確認済み

## 残課題

### 🔴 P0: Search Console が認証エラーで動かない

**現象:**
```
Search Console 取得失敗: invalid_grant: Invalid grant: account not found
```

**試したこと:**
1. ✅ `SEARCH_CONSOLE_SITE_URL=sc-domain:tastas.work` (ドメインプロパティ) → 失敗
2. ✅ Search Console の Google Cloud で API 有効化済み (https://console.cloud.google.com/apis/library/searchconsole.googleapis.com?project=tastas-488506)
3. ✅ ドメインプロパティ `tastas.work` の「ユーザーと権限」に `tastas-api-user@tastas-488506.iam.gserviceaccount.com` 追加 → ユーザーは「委任所有者」状態で動かず
4. ✅ URLプレフィックスプロパティ `https://tastas.work/` を新規作成 (確認済み所有者)
5. ✅ `SEARCH_CONSOLE_SITE_URL=https://tastas.work/` に切り替え
6. ✅ URLプレフィックスプロパティの「ユーザーと権限」にもサービスアカウント追加
7. ❌ それでも同じエラー `invalid_grant: account not found`

**残された仮説:**
- (a) **GA4 用と Search Console 用で必要な OAuth scope が違うが、`google.auth.JWT` が正しく動いていない可能性**
- (b) **サービスアカウントキー (`9cda5cc7d4...`) が GCP 側で何らかの理由で無効化** (ただしGA4は動いているので可能性低い)
- (c) **search-console-client.ts の auth 実装 (`new google.auth.JWT({...})`) が `keyFile` 経路でも GA4 と異なる挙動**

**次にやること:**
1. `src/lib/search-console-client.ts` の auth 実装を、ga-client.ts と同じ `googleapis.options({ keyFilename })` 経路に書き換えて試す
2. 認証だけ単独実行する小さなスクリプトを書いて、`JWT.authorize()` の生エラーを観察
3. それでもダメなら新しいサービスアカウントを作って GA4/SC 両方に追加してみる

**コード位置:**
- `src/lib/search-console-client.ts` L19-49 (auth 実装)
- `src/lib/ga-client.ts` L8-31 (比較用)

### 🟡 P1: 表UI (共有メニュー) がストリーミング直後に出ない

**現象:**
- 「LP30 にどこから来た」など聞くと、表はMarkdown表 + 旧Sheetsボタン (1個のみ) で表示される
- ハードリロードすると T-XXX バッジ + 共有ドロップダウンの新UIに切り替わる
- 新規メッセージは初回も旧UIになる

**原因の仮説:**
- `splitContentByTableMarker` が `sqlTables` propを受け取ったタイミングで再レンダリングしていない
- もしくは done受信時の `setSqlTablesByMessage` が正しく動いていない
- UnifiedMessage の IIFE で stale closure になっている可能性
- DevToolsコンソールのデバッグログ `[unified-message] sqlTables:` がユーザー側で見えていない → そもそも条件分岐に入っていない or React がレンダリングをスキップしている

**コード位置:**
- `src/components/advisor/chat/unified-message.tsx` L186-271 (置換ロジック)
- `src/components/advisor/chat/chat-layout.tsx` L737-797 (done受信時の処理)

**次にやること:**
1. UnifiedMessageに `useEffect` でデバッグログを追加して、`sqlTables` の値が変わるたびに console.log
2. `splitContentByTableMarker` の戻り値が `[{kind:'md', text:本文}]` のみになっていないか確認
3. それでも不明なら useMemo で segments をキャッシュ + 依存配列を厳格にする (React 18 strict mode の再レンダリング問題対策)

### 🟡 P1: 応答が異常に遅い (2分超)

**現象:**
- 簡単な質問でも 100〜170 秒かかる
- loop=1 の TTFB が 100 秒級

**原因 (既知):**
- Anthropic API 側の「loop=1 TTFB 100秒問題」 (HANDOFF.md L227-266 既出)
- 加えて LLM が並列で複数ツール (query_ga4 + query_search_console + get_vercel_deployments 等) を呼び、失敗するとリトライ&コード調査ループに入る

**短期対処案:**
1. **システムプロンプトで「失敗ツールのコード調査はするな」と強く指示**
   - LLMが `search_codebase` / `read_repo_file` で 6ループも調査してる
2. **execute_sql 短絡と同じパターンを query_ga4 系にも適用**
   - 重い loop=1 をスキップして固定文で done
3. **Search Console を `available: false` 扱いにして当面ツールから外す**
   - 認証問題解決まで

**コード位置:**
- `src/lib/advisor/system-prompt.ts` (プロンプト改修箇所)
- `src/lib/advisor/orchestrator.ts` L840-952 (execute_sql 短絡の参考)

## 環境状態

### .env.local 設定
- `DATABASE_URL`, `DIRECT_URL`: `localhost:5432/sworks_dev` (ローカル Docker)
- `ADVISOR_DATA_DATABASE_URL`: 本番 Supabase (`ryvyuxomiqcgkspmpltk`) READ ONLY
- `GA4_PROPERTY_ID=522574288`
- `GOOGLE_APPLICATION_CREDENTIALS=credentials/ga-service-account.json` (新キー配置済み)
- `SEARCH_CONSOLE_SITE_URL="https://tastas.work/"`

### Vercel 環境変数 (本番) — まだ未反映
ユーザーが手動で追加する必要あり:
- `GA_CREDENTIALS_JSON` (JSON全文)
- `SEARCH_CONSOLE_SITE_URL="https://tastas.work/"`
- (CLAUDE.md ルールにより Claude Code は本番環境変数を触らない)

### DB スキーマ変更
- `advisor_chat_tables` テーブル新規作成
- `advisor_sql_audit_logs` テーブル新規作成
- 両方ともローカル Docker のみに push 済み。**本番 Supabase には未反映**

## ブランチ・コミット

ブランチ: `feature/advisor-sql-tool` (mainからの差分大量、PR未作成)

主なコミット (新しい順):
- da90d1c: 表ID紐付け修正 (新規セッションconversationId=null救済) + デバッグログ
- 964db1d: doneイベントで annotatedContent 返却 (リロード不要)
- 0e3a273: Markdown表のT-XXX UI表示 + SC env設定
- ee2339c: get_table / GA4 pageTraffic / Markdown表T-XXX採番
- 6080aef: execute_sql 後 loop=1 短絡 (TTFB 100秒回避)
- 6529c73: 表デザイン統一 + 共有メニュー (Sheets/レポート/URL)
- c4c9b02: セッション紐付け + cron保持ルール統合
- 1745064: execute_sql + add_tables_to_report + SQL承認UI 初期実装
- ec7fdf4: Prismaスキーマ (advisor_chat_tables / advisor_sql_audit_logs) 追加

## 次セッションでの推奨アクション

優先順:
1. **P0 Search Console** — `search-console-client.ts` の auth実装をGA4と同じ `googleapis.options({ keyFilename })` 方式に書き換え試行
2. **P1 表UIの初回反映バグ** — useEffect デバッグ追加 → 原因特定 → 修正
3. **P1 応答遅延** — システムプロンプトに「失敗ツールの調査はやめろ」追加 + query_ga4 系の短絡検討
4. **本番デプロイ準備** — PR作成、Vercel環境変数追加(ユーザー作業)、本番DBへのスキーマpush(ユーザー作業)

## 補足: ユーザー実環境の状態

- ユーザー(川嶋さん)は GA Search Console で `tastas.work` ドメインプロパティの**委任所有者** (確認済み所有者ではない)
- 確認済み所有者は伊藤さん・川嶋さん本人 (オーナー権限)
- DNS は **ムームードメイン** (`dns01.muumuu-domain.com`) で管理されているが、ユーザーは DNS 管理画面へのログイン情報を持っていない
- DNS確認TXTレコード追加には別のメンバー (システム管理者) への依頼が必要

## 補足: 重要な設計決定

- **データ構造化保存(案D)** ではなく **Markdown再パース(案B)** を採用
  - 理由: 実装量を抑える (~400行 vs 1500-3000行)
  - 副作用: LLMが書く表の表記揺れに弱い、桁区切り数値が文字列で残る
  - 数値計算は LLM が Markdown を読んで計算する設計 (get_table経由)

- **表ID `T-001` 形式** は autoincrement の数値IDをゼロ埋め (`T-${id.padStart(3,'0')}`)
  - 全セッション通しの連番、衝突なし

- **共有URL** は `/advisor/t/[token]` 形式、トークンknowing制、30日有効
  - 既存レポート共有 `/advisor/r/[token]` と同じ設計
